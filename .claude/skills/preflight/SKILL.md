---
name: preflight
description: Check this machine can actually run the workshop before lesson 1 costs anyone time — Node, dependencies, an OpenRouter key, and whether the model can really emit a tool call. Use when they say "preflight", "am I set up?", or "check my setup".
---

Read `.agents/preflight.md` and follow it exactly. Run the checks, do not describe them.
